# تحقق من تغطية سيناريوهات الإشعارات الأربعة

## الحالات المطلوبة:

### ✅ 1. طالب يستقبل رسالة فردية من أستاذ
**الشرط في الكود:**
```dart
else if (data['receiver_type'].toString() == "App\\Models\\App_student" &&
    data['sender_type'] == 'App\\Models\\App_teacher' &&
    token != null)
```
- `receiver_type`: `App\Models\App_student` (الطالب هو المستقبل)
- `sender_type`: `App\Models\App_teacher` (الأستاذ هو المرسل)
- `token != null` (المستخدم الحالي طالب)
- **التنقل**: `ChatStudent()` ✓

### ✅ 2. أستاذ يستقبل رسالة فردية من طالب
**الشرط في الكود:**
```dart
else if (teacherToken != null &&
    data['receiver_type'].toString() == "App\\Models\\App_teacher" &&
    data['sender_type'] == 'App\\Models\\App_student')
```
- `receiver_type`: `App\Models\App_teacher` (الأستاذ هو المستقبل)
- `sender_type`: `App\Models\App_student` (الطالب هو المرسل)
- `teacherToken != null` (المستخدم الحالي أستاذ)
- **التنقل**: `ChatPage()` (chat teacher) ✓

### ✅ 3. طالب يستقبل رسالة في group chat
**الشرط في الكود:**
```dart
if (data['receiver_type'].toString() == "App\\Models\\Room" &&
    token != null)
```
- `receiver_type`: `App\Models\Room` (المجموعة)
- `token != null` (المستخدم الحالي طالب)
- **التنقل**: `/gorupchatStudent` ✓

### ✅ 4. أستاذ يستقبل رسالة في group chat
**الشرط في الكود:**
```dart
else if (data['receiver_type'].toString() == "App\\Models\\Room" &&
    teacherToken != null)
```
- `receiver_type`: `App\Models\Room` (المجموعة)
- `teacherToken != null` (المستخدم الحالي أستاذ)
- **التنقل**: `/groupChatTeacher` ✓

---

## ✅ الخلاصة: جميع الحالات الأربعة مغطاة بشكل صحيح!

### منطق العمل:
1. **في الواجهة (Foreground)**: يظهر Snackbar يمكن الضغط عليه للتنقل
2. **في الخلفية (Background)**: يظهر إشعار نظام

### الـ Payload المُرسل يحتوي على:
- `receiver_id`: معرف المستقبل
- `receiver_type`: نوع المستقبل (Room/App_student/App_teacher)
- `sender_id`: معرف المرسل
- `sender_name`: اسم المرسل
- `sender_type`: نوع المرسل
- `group_name`: اسم المجموعة (إن وُجد)

### التفريق بين الحالات:
- **طالب vs أستاذ**: عبر فحص `token` (طالب) vs `teacherToken` (أستاذ)
- **فردي vs مجموعة**: عبر `receiver_type` (Room = مجموعة، App_student/App_teacher = فردي)
- **اتجاه الرسالة**: عبر `sender_type` و `receiver_type` معاً
